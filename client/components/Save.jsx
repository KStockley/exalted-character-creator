// this component will display the `save` block of the character sheet

import React from 'react';
import PropTypes from 'prop-types';

const Save = (props) => (
  <input type="submit" className="save-button" value="Save" />
);

export default Save;
